public class interface Celular {

abstract  boolean Celular (boolean tipo);
abstract boolean camera (boolean tipo);
abstract  boolean fone (boolean tipo);
abstract  boolean volume (boolean tipo);
}
